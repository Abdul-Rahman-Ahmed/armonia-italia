include ../helpers/pug/mixins.pug
doctype html
html(lang="ar" dir="rtl")  
  +head("Menu", pagesPath, "menu")
  body
    header
      .container
        +header(pagesPath)
    .menushow.page
      .container
        +menu
    footer
      .container.footer
        +footer(pagesPath)
    script(src="../js/main.js" type="module")
    script(type="module").
      import {appetizers } from "../js/menu.js";
      show(8, appetizers); 

