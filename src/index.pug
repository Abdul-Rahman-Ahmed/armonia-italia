include ./helpers/pug/mixins.pug
doctype html
html(lang="ar" dir="rtl")  
  +head("Armonia Italia", mainPath)
  body
    header
      .container
        +header(mainPath)
    footer
      .container
        +footer(mainPath)
    script(src="./js/main.js")